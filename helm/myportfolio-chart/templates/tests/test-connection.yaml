apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "myportfolio-chart.fullname" . }}-test-connection"
  labels:
    {{- include "myportfolio-chart.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "myportfolio-chart.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
