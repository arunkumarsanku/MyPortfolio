apiVersion: v1
kind: Service
metadata:
  name: {{ include "myportfolio-chart.fullname" . }}
  labels:
    {{- include "myportfolio-chart.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "myportfolio-chart.selectorLabels" . | nindent 4 }}
